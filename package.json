{
  "name": "db-2026",
  "version": "1.0.0",
  "scripts": {
    "up": "docker-compose --env-file .env up --build -d",
    "down": "docker-compose --env-file .env down",
    "clear": "rm -rf ./data",
    "drop": "sh ./db-client.sh \"DROP SCHEMA public CASCADE; CREATE SCHEMA public; GRANT ALL ON SCHEMA public TO public;\""
  }
}